name: Validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-hacs:
    runs-on: "ubuntu-latest"
    name: HACS
    steps:
      - uses: "actions/checkout@v5"
      - uses: "hacs/action@main"
        with:
          category: integration
